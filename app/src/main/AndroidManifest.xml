<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.umn.trashare">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MemberActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".PengurusActivity" />
        <activity android:name=".YayasanActivity" />
        <activity android:name=".DetailBankSampahActivity" />
        <activity android:name=".UpdateSampahActivity" />
        <activity android:name=".AddSampahActivity" />
        <activity android:name=".RegisterNasabahActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DetailEventYayasanActivity" />
        <activity android:name=".AddPrizeActivity" />
        <activity android:name=".DetailPrizeYayasanActivity" />
        <activity android:name=".FormRegistrasiBankSampahActivity" />
        <activity android:name=".PoinSayaFragment" />
        <activity android:name=".HistoryPoinFragment" />
        <activity android:name=".RedeemPrizeActivity" />
        <activity android:name=".DetailRedeemPrizeActivity" />
        <activity android:name=".DetailNotifikasiMemberActivity" />
        <activity android:name=".DetailEventPengurusActivity" />
        <activity android:name=".GantiPasswordActivity" />
        <activity android:name=".AddEventPengurusActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DetailBSMapActivity"
            android:label="@string/title_activity_detail_bsmap" />
        <activity
            android:name=".PoinMemberActivity"
            android:label="@string/title_activity_poin_member"
            android:theme="@style/AppTheme" />
        <activity android:name=".EditProfileActivity"></activity>
    </application>

</manifest>